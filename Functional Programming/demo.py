"""
its modern approach to deduce a program length.

    lambda function
    map
    filter
    list comprehension

1. Lambda Function
    anonymous function (unknown function)

    Lambda functions in Python, often referred to as anonymous functions, are small, unnamed functions defined using the
    lambda keyword. They are typically used for short, throwaway functions.

    syntax:
        variable_name = lambda arguments : operation





"""

f = lambda num1, num2: num1 + num2
print(f(10, 20))
